{{- $profile := promptChoiceOnce . "profile" "Profile" (list "work" "lp" "sp") }}

git:
  autoCommit: true
  autoPush: true

data:
  profile: {{ $profile }}

  {{ include (printf "profiles/%s/.chezmoidata/dock.yaml" $profile) | fromYaml | toYaml }}
